ToDo List
=========

Manually maintained ToDo List.


- Work out why IPv6 and AAAA lookups fail.
- Read system ``/etc/hosts`` if it exists and use to pre-populate zones.
- Make the use of Redis optional? (requires an api)
